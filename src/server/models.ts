export interface UserDetails {
    name: string;
    username: string;
}

export interface PhotoBlob {
    blobName: string;
    text: string;
}

export interface JsonBlobData {
    blobName: string;
    imageUrl: {};
}
